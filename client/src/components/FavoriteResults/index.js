import React from "react";
import "./style.css";

function FavoriteResults(props) {
  console.log(props.user.favorites);

  return (
    <ul>
      <li>Test</li>
    </ul>
  );
}

export default FavoriteResults;
